# AI 에이전트 작업 지침 (AGENTS.md)

이 문서는 이 프로젝트에서 함께 작업하는 AI 에이전트(예: Antigravity)가 개발, 협업, 환경 관리를 수행할 때 반드시 준수해야 하는 규칙과 가이드라인을 정의합니다.

---

## 1. 언어 및 소통 규칙
- **문서 및 코드 주석**: 프로젝트 내의 개발 문서와 코드 주석은 모두 **한글**로 작성합니다.
- **기존 주석 보존**: 코드를 수정하거나 리팩토링할 때, 수정 대상 외의 기존 한글 주석과 설명은 훼손하지 않고 그대로 보존합니다.

## 2. Python 환경 및 패키지 관리 (`uv`)
이 프로젝트는 Python 가상 환경 및 패키지 관리에 `uv`를 사용합니다.
- **명령어 실행**: `python main.py` 대신 반드시 `uv run main.py`를 사용하여 가상 환경 내에서 스크립트를 실행합니다.
- **패키지 추가/삭제**:
  - 패키지가 필요할 경우 `pip install`을 사용하지 않고 `uv add <package>`를 사용하여 `pyproject.toml`과 `uv.lock`을 갱신합니다.
  - 불필요한 패키지는 `uv remove <package>`를 통해 제거합니다.
- **가상 환경 활성화/싱크**: 에이전트는 프로젝트 실행 전 `uv sync`를 통해 종속성이 최신 상태인지 확인합니다.

## 3. Git 커밋 규칙
- **한글 커밋 메시지**: Git 커밋 시 작성하는 모든 커밋 메시지는 **한글**로 명확하게 작성합니다.
- **커밋 메시지 형식**: 가능한 경우 아래와 같은 접두사를 사용하여 명확하게 구분합니다.
  - `[Feat]`: 새로운 기능 추가
  - `[Fix]`: 버그 수정
  - `[Docs]`: 문서 생성 및 수정 (`AGENTS.md` 생성은 `[Docs]` 또는 `[Feat]` 사용 가능)
  - `[Refactor]`: 코드 리팩토링
  - `[Chore]`: 빌드 업무 수정, 패키지 매니저 설정 등
- **예시**: `git commit -m "[Docs] AI 에이전트 개발 지침서(AGENTS.md) 추가"`
